// start the server

const app=require("./src/app")


app.listen(3000,()=>{
  console.log("Server Started on PORT 3000!!")
})

// start the server

const app=require("./src/app")


app.listen(3000,()=>{
  console.log("Server Started on PORT 3000!!")
})